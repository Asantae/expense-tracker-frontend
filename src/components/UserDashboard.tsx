import { Typography } from '@mui/material';
import React from 'react';

const UserDashboard = () => {
    
    return <Typography>Welcome to the UserDashboard!</Typography>;
};

export default UserDashboard;